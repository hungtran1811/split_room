{
  "name": "split-room-functions",
  "private": true,
  "version": "0.1.0",
  "description": "Split Room Cloud Functions — month close + mail outbox",
  "main": "src/index.js",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test src/monthClose.test.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.3.0",
    "split_room": "file:.."
  }
}
