{
  "name": "split_room",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --exclude tests/firestore.rules.test.js",
    "test:watch": "vitest",
    "test:rules": "node scripts/run-firestore-emulator.mjs exec",
    "emu": "node scripts/run-firestore-emulator.mjs start",
    "backfill:rents": "node scripts/backfill-rents.mjs",
    "verify:data": "node scripts/verify-rent-data.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase-admin": "^13.5.0",
    "firebase-tools": "^14.22.0",
    "vite": "^7.2.4",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "firebase": "^12.8.0"
  }
}
